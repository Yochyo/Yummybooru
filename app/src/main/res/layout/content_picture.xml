<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/drawer_picture"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:showIn="@layout/activity_picture"
        android:background="@color/dark_grey"
        tools:context=".layout.PictureActivity"
        tools:openDrawer="end">


    <android.support.design.widget.NavigationView
            android:id="@+id/nav_view_picture"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="end"
            android:fitsSystemWindows="true"
            android:background="@color/dark_grey"
            app:itemTextColor="@color/white"
            app:headerLayout="@layout/nav_header_picture"/>

    <ImageView android:layout_width="match_parent" android:layout_height="match_parent"
               android:background="@color/transparent" android:id="@+id/image_view"/>

</android.support.v4.widget.DrawerLayout>